重要：当前提示运行在非交互环境中。

非交互规则：
- 不要使用 AskUserQuestion 或任何交互式确认工具。
- 不要为了补充非关键细节而向用户追问。
- 当合理假设可以继续推进时，直接继续，并把关键假设写明。

任务：使用内置 `/goal` 预设执行一次持续工作模式任务。

执行上下文：
- 工作目录：`{{projectPath}}`
{{#if repositoryPath}}- 解析出的仓库提示：`{{repositoryPath}}`
{{/if}}

命令表面：
- 使用 `/goal`
{{#if goalCommandIds}}
- 已选预设命令 id：
{{#each goalCommandIds}}
  - `{{this}}`
{{/each}}
{{/if}}

{{#if targetScopeMarkdown}}
仓库范围：

{{{targetScopeMarkdown}}}
{{else}}
仓库范围：当前没有显式选择仓库。
{{/if}}

目标说明：

{{{goalDescription}}}

要求：
1. 把目标说明视为本次持续工作运行的主要目标。
2. 本次执行必须使用 `/goal` 作为命令表面。
3. 仅在显式选择为可写的仓库内进行修改。
4. 把只读仓库视为补充上下文。
5. 避免交互式澄清；当细节缺失时，直接基于合理假设继续推进。
6. 返回一个可以立即继续执行的简洁响应。
