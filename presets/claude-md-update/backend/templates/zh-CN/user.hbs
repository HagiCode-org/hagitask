重要：此提示可能运行在非交互环境中。

非交互规则：
- 不要使用 AskUserQuestion 或任何交互式确认工具。
- 当需要可选决策时，选择合理默认值，并记录该假设。
- 跳过可选确认，直接继续执行。

任务：根据下面解析后的作用域，更新所选项目的代理文档。以 `AGENTS.md` 为主要数据源进行更新；`CLAUDE.md` 仅保持为极简重定向入口。

执行上下文：
- 工作目录：`{{projectPath}}`
{{#if repositoryPath}}- 解析得到的仓库提示：`{{repositoryPath}}`
{{/if}}- 解析得到的作用域键：`{{scope}}`

目标文件范围：

{{{targetScopeMarkdown}}}

要求：
1. 修改前先阅读现有的 `AGENTS.md` 与 `CLAUDE.md`。
2. 变更必须严格限制在目标文件范围中明确列出的文件。
3. 将 `AGENTS.md` 视为**主要文档数据源**。所有实质性指令、命令、约定和代理行为约束都放在 `AGENTS.md` 中。
4. 保持 `CLAUDE.md` **极简**——仅作为重定向，说明其内容在 `AGENTS.md` 中。参考模式：
   ```
   # Claude 配置
   此文件指向 [AGENTS.md](./AGENTS.md)，后者是项目特定 AI 助手配置的唯一事实来源。
   ```
5. 当 `scope` 为 `project-only` 或 `root-only` 时，更新根目录的 `AGENTS.md`（主要内容）和 `CLAUDE.md`（重定向）。
6. 当 `scope` 为 `root-and-children` 时，更新范围块中列出的所有 `AGENTS.md` / `CLAUDE.md` 文件，对每个目标都遵循 AGENTS 为主的模式。
7. 保留仍然准确的现有说明；只有在被更好的当前说明替换时，才移除过时内容。
8. 优先复用相邻仓库已有的文档约定，不要发明新的维护格式。
9. 若列出的文件缺失，或某个子仓库还没有现成维护文档，必须明确记录你的假设。
10. 最终返回简洁总结，说明你修改了哪些文件，以及实际采用了哪个作用域。

建议执行流程：
1. 检查所选项目根目录，以及范围中列出的子仓库文件。
2. 读取每个目标的现有 `AGENTS.md` 与 `CLAUDE.md` 内容。
3. 对照仓库当前真实的工具链和工作流，判断现有文档哪里过时。
4. **先构建 `AGENTS.md`**，将其作为全面的唯一事实来源。
5. **再更新 `CLAUDE.md`**，保持为指向 `AGENTS.md` 的极简重定向。
6. 总结结果以及仍未解决的缺口。
